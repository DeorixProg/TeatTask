name: CI Pipeline

# 🔁 Тригери: при пуші в main + можливість ручного запуску
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest  

    steps:
      
      - name: Checkout Repository
        uses: actions/checkout@v3

      
      - name: Install Required Software
        run: |
          sudo apt update
          sudo apt install -y curl

      
      - name: Install Dependencies
        run: |
          npm ci  # або npm install

     
      - name: Build Project
        run: |
          npm run build  # зміни на свою команду, якщо треба

  
      - name: Run Unit Tests
        run: |
          npm test  # адаптуй під свій стек

      
      - name: Commit & Push Changes (if any)
        run: |
          git config --global user.email "actions@github.com"
          git config --global user.name "GitHub Actions"
          git add .
          git diff-index --quiet HEAD || git commit -m "Auto update by CI"
          git push
